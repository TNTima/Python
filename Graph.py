class Node:
    def __init__ (self, links, value = None):
        assert links is dict
        node, value links.items.pop() 

        if value is not None:
            self.value = value
        self.links = links;
    
    @staticmethod
    def join (node1, node2, link value)
        
        
